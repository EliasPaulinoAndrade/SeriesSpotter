import SwiftUI

struct SeriesEmptyView: View {
    var body: some View {
        Text("Couldnt find any series in the search. You can try change a bit your search terms.")
            .padding(Space.base03)
    }
}
